# this is run _before_ the node is created (so it doesn't exist yet)
# it is only run on scene load, and _once_ (not on interactive node creation)
#

if False:
    N = kwargs['node']
    M = N.hdaModule()
    M.msg('PreFirstCreate: %s' % N.path())

#print "kwargs:", kwargs
#print " - $HIP: %s" % hou.expandString("$HIP")

