#icon: COP2/frontface
#context: obj
#type: node

= Camera Plane qL =

*Version 0.0.1*

"""Convenience wrapper around the [Camera Plane SOP|Node:sop/qLib::camera_plane_ql::1]."""

If not indicated otherwise the parameters are direct references to the respective parameters
of the [Camera Plane SOP|Node:sop/qLib::camera_plane_ql::1].

@Parameters
    Viewport Display:
        Displays the plane in the viewports.
    Renderable:
        Makes the plane renderable.

@Release_Notes Release Notes

    Version 0.0.1:
        Initial release.